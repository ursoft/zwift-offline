syntax = "proto2";
option optimize_for = LITE_RUNTIME; //as in original PC build
package protobuf;
message HashSeed {
    optional uint32 seed1 = 1;
    optional uint32 seed2 = 2;
    optional uint64 expiry_date = 3;
}
message HashSeeds {
    repeated HashSeed seeds = 1;
}
